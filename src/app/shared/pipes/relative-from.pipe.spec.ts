import { RelativeFromPipe } from './relative-from.pipe';

describe('RelativeFromPipe', () => {
  it('create an instance', () => {
    const pipe = new RelativeFromPipe();
    expect(pipe).toBeTruthy();
  });
});
